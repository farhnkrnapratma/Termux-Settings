# Termux
alias upd="apt update && apt upgrade" # or "apt update && apt upgrade -y"
alias ins="apt install" # or "apt install -y"
alias b="cd .."
alias clr="clear"
alias lsa="ls -a"
alias out="logout"
alias del="rm -rf"
# Other
alias mic="micro" # Micro code editor
